<div class="container py-4" style="max-width: 700px;">
  <%= link_to "← #{@work_item.room.name}", @work_item.room, class: "od-back-link" %>
  <div class="od-page-title">Modifier le poste</div>

  <div class="od-form-card">
    <%= render "form" %>
  </div>
</div>
