<% content_for :head do %>
<style>
  body { background-color: #FFFFFF; }

  .project-card {
    border: 1px solid #C8C4BC;
    border-radius: 10px;
    background: #fff;
    text-decoration: none;
    color: inherit;
    display: block;
    transition: border-color 0.15s ease, transform 0.15s ease, box-shadow 0.15s ease;
  }
  .project-card:hover {
    border-color: #2C2A25;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(44,42,37,0.08);
    color: inherit;
    text-decoration: none;
  }

  .badge-brouillon { background-color: #EDE9E0; color: #4A4640; }
  .badge-en-cours  { background-color: #D4EDDA; color: #2D6A4F; }
  .badge-termine   { background-color: #D0E8F5; color: #1A5C80; }
  .badge-refuse    { background-color: #F8D7DA; color: #842029; }

  .card-total {
    font-size: 1.4rem;
    font-weight: 700;
    color: #2C2A25;
    letter-spacing: -0.5px;
  }
  .card-meta { color: #4A4640; font-size: 0.85rem; }

  .btn-primary-dark {
    background-color: #2563eb;
    border-color: #2563eb;
    color: #fff;
    border-radius: 8px;
    font-weight: 500;
    padding: 0.5rem 1.1rem;
  }
  .btn-primary-dark:hover {
    background-color: #1B4FC2;
    border-color: #1B4FC2;
    color: #fff;
  }

  .page-title { font-size: 1.6rem; font-weight: 700; color: #2C2A25; }
  .project-count { color: #4A4640; font-size: 0.95rem; margin-top: 2px; }

  .empty-state-icon { font-size: 3.5rem; line-height: 1; }

  .project-card-wrapper { position: relative; }
  .btn-delete-project {
    position: absolute;
    top: 8px;
    right: 8px;
    z-index: 10;
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: rgba(255,255,255,0.85);
    border: none;
    color: #C5BFB5;
    font-size: 1.1rem;
    line-height: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    transition: color 0.12s, background 0.12s;
    cursor: pointer;
  }
  .btn-delete-project:hover {
    color: #842029;
    background: #F8D7DA;
  }
  .project-card-photo {
    width: 100%;
    height: 130px;
    object-fit: cover;
    border-radius: 9px 9px 0 0;
    display: block;
  }
  .project-card-photo-placeholder {
    width: 100%;
    height: 130px;
    border-radius: 9px 9px 0 0;
    background: #F0EDE8;
    display: flex;
    align-items: center;
    justify-content: center;
  }
</style>
<% end %>

<div data-controller="delete-confirm">

  <div class="container py-4" style="max-width: 1100px;">
    <%# Header %>
    <div class="d-flex justify-content-between align-items-start mb-4">
      <div>
        <div class="page-title">Mes projets</div>
        <div class="project-count"><%= pluralize(@projects.size, "projet(s)") %></div>
      </div>
      <%= link_to wizard_choose_path, class: "btn btn-primary-dark" do %>
        + Nouveau projet
      <% end %>
    </div>

    <% if @projects.any? %>
      <div class="row g-3">
        <% @projects.each do |project| %>
          <% status_label, status_class = case project.status
             when "in_progress"     then ["Projet en cours",  "badge-en-cours"]
             when "quote_requested" then ["Devis demandé",    "badge-brouillon"]
             when "quote_received"  then ["Devis reçu",       "badge-termine"]
             when "archived"        then ["Archivé",           "badge-refuse"]
             else                        ["Projet en cours",  "badge-en-cours"]
             end %>
          <div class="col-md-4">
            <div class="project-card-wrapper h-100">
              <button class="btn-delete-project"
                      title="Supprimer ce projet"
                      data-action="click->delete-confirm#show"
                      data-delete-path="<%= project_path(project) %>"
                      data-delete-name="<%= project.location_zip.presence || "Sans localisation" %>">
                &times;
              </button>
              <%= link_to project, class: "project-card h-100" do %>
                <%# Photo banner — image or placeholder %>
                <% if project.photo_url.present? %>
                  <img src="<%= project.photo_url %>"
                       alt="<%= project.name.presence || project.location_zip %>"
                       class="project-card-photo" />
                <% else %>
                  <div class="project-card-photo-placeholder">
                    <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="#C8C4BC" viewBox="0 0 16 16">
                      <path d="M8.707 1.5a1 1 0 0 0-1.414 0L.646 8.146a.5.5 0 0 0 .708.708L2 8.207V13.5A1.5 1.5 0 0 0 3.5 15h9a1.5 1.5 0 0 0 1.5-1.5V8.207l.646.647a.5.5 0 0 0 .708-.708L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293zM13 7.207V13.5a.5.5 0 0 1-.5.5h-2v-3a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5v3h-2a.5.5 0 0 1-.5-.5V7.207l4-4z"/>
                    </svg>
                  </div>
                <% end %>

                <div class="p-4">
                <%# Title + badge below %>
                <div class="mb-3">
                  <div style="font-size: 1.05rem; font-weight: 600; color: #2C2A25; margin-bottom: 0.4rem;">
                    <%= project.location_zip.presence || "Sans localisation" %>
                  </div>
                  <span class="badge <%= status_class %> fw-normal" style="font-size: 0.8rem; padding: 0.35em 0.75em; border-radius: 20px;">
                    <%= status_label %>
                  </span>
                </div>

                <%# Price range Éco → Premium %>
                <% eco_total     = project.total_incVAT_for_standing(1) %>
                <% premium_total = project.total_incVAT_for_standing(3) %>
                <div class="card-total mb-1">
                  <% if eco_total > 0 && premium_total > 0 %>
                    <%= number_to_currency(eco_total, unit: "€", format: "%n %u", separator: ",", delimiter: "\u202F", precision: 0) %>
                    <span style="font-weight: 400; font-size: 1rem; color: #9B9588; margin: 0 0.2rem;">—</span>
                    <%= number_to_currency(premium_total, unit: "€", format: "%n %u", separator: ",", delimiter: "\u202F", precision: 0) %>
                  <% else %>
                    — €
                  <% end %>
                </div>
                <div class="card-meta mb-3">Fourchette TTC estimée (Éco → Premium)</div>

                <%# Room count + surface %>
                <div class="card-meta">
                  <% parts = [] %>
                  <% parts << "#{project.room_count}p" if project.room_count.present? %>
                  <% parts << "#{project.total_surface_sqm&.to_i} m²" if project.total_surface_sqm.present? %>
                  <% unless parts.empty? %>
                    <span><%= parts.join(" · ") %></span>
                    <span class="mx-1">·</span>
                  <% end %>
                  <span>Modifié <%= time_ago_in_words(project.updated_at) %></span>
                </div>
                </div><%# /p-4 %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

    <% else %>
      <%# Empty state %>
      <div class="d-flex flex-column align-items-center justify-content-center py-5 mt-4">
        <div class="empty-state-icon mb-3">🏗️</div>
        <h5 style="color: #2C2A25; font-weight: 600;">Aucun projet pour le moment</h5>
        <p class="text-center mb-4" style="color: #4A4640; max-width: 360px;">
          Créez votre première estimation de travaux en quelques clics.
        </p>
        <%= link_to "+ Nouveau projet", wizard_choose_path, class: "btn btn-primary-dark" %>
      </div>
    <% end %>

    <%# Archives section %>
    <% if @archived_projects.any? %>
      <div class="mt-5 pt-4" style="border-top: 1px solid #E8E4DC;">
        <div class="d-flex align-items-center gap-2 mb-3">
          <div class="page-title" style="font-size: 1.1rem;">Archives</div>
          <span style="font-size: 0.85rem; color: #9B9588;"><%= pluralize(@archived_projects.size, "projet(s)") %></span>
        </div>
        <div class="row g-3">
          <% @archived_projects.each do |project| %>
            <div class="col-md-4">
              <div class="project-card-wrapper h-100" style="opacity: 0.65;">
                <button class="btn-delete-project"
                        title="Supprimer ce projet"
                        data-action="click->delete-confirm#show"
                        data-delete-path="<%= project_path(project) %>"
                        data-delete-name="<%= project.location_zip.presence || "Sans localisation" %>">
                  &times;
                </button>
                <%= link_to project, class: "project-card h-100" do %>
                  <% if project.photo_url.present? %>
                    <img src="<%= project.photo_url %>"
                         alt="<%= project.location_zip %>"
                         class="project-card-photo" style="filter: grayscale(40%);" />
                  <% else %>
                    <div class="project-card-photo-placeholder">
                      <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="#C8C4BC" viewBox="0 0 16 16">
                        <path d="M8.707 1.5a1 1 0 0 0-1.414 0L.646 8.146a.5.5 0 0 0 .708.708L2 8.207V13.5A1.5 1.5 0 0 0 3.5 15h9a1.5 1.5 0 0 0 1.5-1.5V8.207l.646.647a.5.5 0 0 0 .708-.708L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293zM13 7.207V13.5a.5.5 0 0 1-.5.5h-2v-3a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5v3h-2a.5.5 0 0 1-.5-.5V7.207l4-4z"/>
                      </svg>
                    </div>
                  <% end %>
                  <div class="p-4">
                    <div class="mb-3">
                      <div style="font-size: 1.05rem; font-weight: 600; color: #2C2A25; margin-bottom: 0.4rem;">
                        <%= project.location_zip.presence || "Sans localisation" %>
                      </div>
                      <span class="badge badge-refuse fw-normal" style="font-size: 0.8rem; padding: 0.35em 0.75em; border-radius: 20px;">
                        Archivé
                      </span>
                    </div>
                    <% eco_total     = project.total_incVAT_for_standing(1) %>
                    <% premium_total = project.total_incVAT_for_standing(3) %>
                    <div class="card-total mb-1">
                      <% if eco_total > 0 && premium_total > 0 %>
                        <%= number_to_currency(eco_total, unit: "€", format: "%n %u", separator: ",", delimiter: "\u202F", precision: 0) %>
                        <span style="font-weight: 400; font-size: 1rem; color: #9B9588; margin: 0 0.2rem;">—</span>
                        <%= number_to_currency(premium_total, unit: "€", format: "%n %u", separator: ",", delimiter: "\u202F", precision: 0) %>
                      <% else %>
                        — €
                      <% end %>
                    </div>
                    <div class="card-meta mb-3">Fourchette TTC estimée (Éco → Premium)</div>
                    <div class="card-meta">
                      <% parts = [] %>
                      <% parts << "#{project.room_count}p" if project.room_count.present? %>
                      <% parts << "#{project.total_surface_sqm&.to_i} m²" if project.total_surface_sqm.present? %>
                      <% unless parts.empty? %>
                        <span><%= parts.join(" · ") %></span>
                        <span class="mx-1">·</span>
                      <% end %>
                      <span>Archivé <%= time_ago_in_words(project.updated_at) %></span>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <%# Modal de confirmation de suppression — à l'intérieur du controller Stimulus %>
  <div class="modal fade" id="deleteConfirmModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width: 420px;">
      <div class="modal-content" style="border: 1px solid #C8C4BC; border-radius: 12px; box-shadow: 0 8px 32px rgba(44,42,37,0.12);">
        <div class="modal-body p-4">

          <div class="d-flex align-items-center gap-3 mb-3">
            <div style="width: 40px; height: 40px; border-radius: 10px; background: #F8D7DA; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="#842029" viewBox="0 0 16 16">
                <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5m3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0z"/>
                <path d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4zM2.5 3h11V2h-11z"/>
              </svg>
            </div>
            <div>
              <div style="font-weight: 700; color: #2C2A25; font-size: 1rem;">Supprimer le projet</div>
              <div style="color: #4A4640; font-size: 0.85rem; margin-top: 2px;">
                "<span data-delete-confirm-target="nameDisplay" style="color: #2C2A25; font-weight: 500;"></span>"
              </div>
            </div>
          </div>

          <p style="color: #4A4640; font-size: 0.88rem; margin: 0 0 1.25rem;">
            Cette action est irréversible. Toutes les pièces et les postes associés à ce projet seront définitivement supprimés.
          </p>

          <div class="d-flex gap-2 justify-content-end">
            <button type="button"
                    data-bs-dismiss="modal"
                    style="border: 1px solid #C8C4BC; background: #fff; color: #4A4640; border-radius: 8px; font-size: 0.88rem; padding: 0.45rem 1rem; cursor: pointer;">
              Annuler
            </button>
            <button type="button"
                    data-action="click->delete-confirm#confirm"
                    style="background: #842029; color: #fff; border: none; border-radius: 8px; font-size: 0.88rem; font-weight: 500; padding: 0.45rem 1rem; cursor: pointer;">
              Supprimer définitivement
            </button>
          </div>

        </div>
      </div>
    </div>
  </div>

  <%# Formulaire caché utilisé par le controller pour soumettre le DELETE %>
  <%= form_with url: "#", method: :delete,
        data: { delete_confirm_target: "form" },
        style: "display: none;" do |f| %>
    <%= f.submit "Supprimer", style: "display: none;" %>
  <% end %>

</div>
